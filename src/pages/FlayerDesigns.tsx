import { MiniMap } from "@/components/shared/MiniMap";
import { flayersImages } from "@/lib/constants/data";

const FlayerDesigns = () => {
  return <MiniMap projectImages={flayersImages} />;
};

export default FlayerDesigns;
